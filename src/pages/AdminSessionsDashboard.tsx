import { AdminSessionsPage } from '@/features/sessions';

/** /configuracion/sesiones — thin route wrapper; implementation lives in features/sessions. */
export default function AdminSessionsDashboard() {
  return <AdminSessionsPage />;
}
