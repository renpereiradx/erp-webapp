import { UserDetailPage } from '@/features/users';

/** /configuracion/usuarios/:id — thin route wrapper; implementation lives in features/users. */
export default function UserDetailedProfile() {
  return <UserDetailPage />;
}
