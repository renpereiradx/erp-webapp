import { UsersPage } from '@/features/users';

/** /configuracion/usuarios — thin route wrapper; implementation lives in features/users. */
export default function UserManagementList() {
  return <UsersPage />;
}
